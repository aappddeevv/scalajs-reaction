name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
        fail-fast: false
    steps:
    - uses: actions/checkout@v2
    - uses: olafurgp/setup-scala@v10
    - name: Compile
      run: sbt fastOptJS
